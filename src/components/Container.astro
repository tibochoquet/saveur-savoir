---
interface Props {
  class?: string;
  wide?: boolean;
}

const { class: className = "", wide = false } = Astro.props;
---

<div class:list={["mx-auto w-full px-6 sm:px-8 lg:px-12", wide ? "max-w-7xl" : "max-w-5xl", className]}>
  <slot />
</div>
